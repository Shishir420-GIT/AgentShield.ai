# Prometheus configuration for AgentShield monitoring

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'agentshield'
    static_configs:
      - targets: ['agentshield:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
